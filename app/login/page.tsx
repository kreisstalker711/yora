import type { Metadata } from "next";
import LoginPage from "@/src/app/login/page";

export const metadata: Metadata = {
  title: "Login | Yora",
  description: "Sign in to your Yora account.",
};

export default LoginPage;
